[unstable]
codegen-backend = true

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-Clink-arg=-fuse-ld=/usr/bin/mold", "-Zthreads=8", "-Zshare-generics=y"]

[target.x86_64-pc-windows-msvc]
# Increase default stack size to avoid running out of stack
# space in debug builds. The size matches Linux's default.
rustflags = ["-C", "link-arg=/STACK:8000000"]

[target.aarch64-pc-windows-msvc]
# Increase default stack size to avoid running out of stack
# space in debug builds. The size matches Linux's default.
rustflags = ["-C", "link-arg=/STACK:8000000"]

[profile.dev]
lto = "thin"

[profile.profiling]
inherits = "release"
incremental = true
debug = true

[profile.dev.package."*"]
codegen-backend = "llvm"

[profile.profiling.package."*"]
codegen-backend = "llvm"
