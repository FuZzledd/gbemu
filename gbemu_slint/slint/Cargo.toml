[package]
name = "slint_generated"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = { workspace = true, features = ["backend-qt"] }


[build-dependencies]
slint-build = { workspace = true }
